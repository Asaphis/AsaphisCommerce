PORT=3000

# ======================================
# FRONTEND-ONLY CONFIGURATION
# ======================================
# This app is now a frontend-only application that connects to a separate backend API

# API Configuration
# Replace with your actual backend API URL
NEXT_PUBLIC_API_URL=http://localhost:4000

# Public Environment Variables
NEXT_PUBLIC_CURRENCY=$
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_aW1tZW5zZS1jaXZldC0zLmNsZXJrLmFjY291bnRzLmRldiQ

# Clerk Authentication (Frontend Only)
CLERK_SECRET_KEY=sk_test_qjqMtymVkiahNiJzfvsjZxjG8p1coujnYGHQlccUiA

# ======================================
# REMOVED BACKEND DEPENDENCIES
# ======================================
# The following variables are no longer needed as the backend logic has been removed:
# - MONGODB_URI (Database connection moved to separate backend)
# - INNGEST_EVENT_KEY (Background jobs moved to separate backend)
# - INNGEST_SIGNING_KEY (Background jobs moved to separate backend)
# - CLERK_WEBHOOK_SECRET (Webhook handling moved to separate backend)
# - CLOUDINARY_* (File upload handling moved to separate backend)
